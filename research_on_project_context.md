# Research On — 프로젝트 컨텍스트 정리

**대상 조직**: 이트너스 BPO사업본부
**시스템 목적**: 주간 뉴스레터 자동 생성/발송 + 사내 리서치 허브(수동 키워드 조사 도구)
**구성**: n8n 워크플로우 5개(뉴스레터 1/2단계, 뉴스크롤링 1/2단계, 클라이언트 동향 수집) + 웹 프론트엔드(Research On 리서치허브, index.html+styles.css+js/*.js로 분리) + Supabase(인증/DB — 2026-07-25부터 n8n의 데이터 저장·맥락 문서도 전부 이관됨). Google Sheets/Docs는 백업용으로만 남아있고 더 이상 실사용 경로가 아님(7번 섹션 35번 항목 참고).

> 이 문서는 최초 작성 이후 진행된 세션에서 있었던 변경사항을 반영해 갱신되었습니다. 최신 변경사항은 각 섹션에 통합했고, 연대기(7번 섹션)에 새 항목을 이어붙였습니다.

---

## 1. 전체 시스템 구성

| 컴포넌트 | 역할 |
|---|---|
| **뉴스레터 1단계** (n8n) | 매주 회사·클라이언트 관련 뉴스 + 트렌드 뉴스레터를 수집·평가해서 Google Sheets에 후보로 저장 |
| **뉴스레터 2단계** (n8n) | 1단계가 쌓아둔 후보 중 최근 것을 골라 실제 발송용 뉴스레터 HTML을 생성 + 관리자 승인(HITL) + 예약 발송 |
| **뉴스크롤링 1단계** (n8n) | 리서치 허브에서 사용자가 직접 키워드 검색할 때 쓰는 백엔드 (~20건 수집) |
| **뉴스크롤링 2단계** (n8n) | 리서치 허브에서 사용자가 선택한 자료를 심층 분석해 AI 인사이트 리포트를 생성 |
| **Research On 리서치허브** (HTML/JS) | 위 4개 워크플로우를 호출하는 프론트엔드. Supabase 인증, 단일 페이지 앱(SPA) 구조, 모바일 반응형 지원 |

---

## 2. 뉴스레터 1단계 — 후보 수집 프로세스

세 개의 하위 파이프라인이 병렬로 돌아가며, 각자 Google Sheets(`newsletter_research`)에 후보를 저장한다.

### 3-A / 3-B 파이프라인 (CLIENT_WATCH / BUSINESS_WORK)
```
검색 Job 생성(키워드 목록 기반, 최근 7일 날짜범위)
  → Naver + Serper 검색
  → 결과 통합 + 중복 제거(클러스터링)
  → 룰 기반 1차 점수화(target 매칭, DS/DX 필터, 발행일 등)
  → 발행일 10일 초과 기사 하드 컷
  → 점수순 정렬 후 섹션당 상위 16개만 추려 AI에게 전달
  → AI가 실제 이슈인지 판단(계열사 제외, 원문제목 재작성 등) 후 최대 6개 선정
  → CL4 이상 보직자(센터장·그룹장·사업부장·임원)를 겨냥한 작성 수준 지침 적용
    (요약 2~3문장 + 구체적 수치·시점 포함, 시사점은 "~기회입니다" 식 상투어 금지하고
     BPO사업본부 관점의 검토·논의 포인트를 담도록 지시)
  → Sheets 저장
```

### 3-C 파이프라인 (PEOPLE_CULTURE)
```
Gmail에서 최근 7일 트렌드레터 수집(뉴닉·캐릿·풋풋레터·까탈로그, 발신자 4곳)
  → 관리성 메일(인증메일·구독확인 등) 제외
  → 본문 정제 + 광고/협찬 블록 제거(by. 퍼블리 등) + 6000자 단위로 청크 분할(자르지 않고 전부 전달)
  → AI가 트렌드 후보 추출(최대 8개), search_query는 반드시 한국어로 작성하도록 지시
    (영문 트렌드명(Maxxing, Touch grass 등)을 그대로 쓰면 한국 뉴스 검색에서 결과가 거의
     안 나오는 문제가 있었음 — 프롬프트 지시 + 코드 레벨 안전장치 이중 적용)
  → Naver + Serper 뉴스 검색으로 후보 검증(예전엔 Serper만 사용했음)
  → 검색 결과 관련성 필터링: 키워드에서 "트렌드/소비/문화" 등 범용 단어를 뺀 핵심 토큰이
    제목+요약에 전부 등장하는지 확인 (Naver/Serper가 단어 단위로 느슨하게 매칭해서
    무관한 기사가 섞여 들어오는 문제 방지)
  → 관련 뉴스를 못 찾은 후보만 추가로 SNS(인스타그램/틱톡/threads/X) 대상 Serper 일반검색 시도
  → 검증된 자료만으로 최종 분석 작성 — 구체적 사례(예: "맥싱"이면 "파이버맥싱" 같은 하위 유형)를
    반드시 포함하도록 지시
  → organization_view(조직 운영 시사점)를 채용·유지·조직문화·세대 소통·복리후생 관점에서
    최소 2문장으로 필수 작성하도록 강화 (예전엔 "짧게 한 줄 또는 근거 약하면 빈 문자열"이었음 —
    이 때문에 실제 발송본에서 People & Culture 섹션에 조직 시사점이 통째로 빠져있었음)
  → Serper 응답의 imageUrl(base64 이미지 통째) 등 불필요한 대용량 필드는 이후 단계로 넘기기 전 제거
  → Sheets 저장
```

---

## 3. 뉴스레터 2단계 — 최종 발송본 생성 및 관리자 승인(HITL)

```
[A] Sheets에서 최근 7일 후보 정규화(섹션별 slice)
  → 회사 맥락 문서(900자, "본부 맥락") 읽기
  → 4개 꼭지(CLIENT WATCH·RESEARCH ON INSIGHT·BUSINESS & WORK·PEOPLE & CULTURE) 편집 프롬프트 생성
    (CL4+ 보직자 대상 작성 수준 지침 동일 적용, PEOPLE & CULTURE도 이제 implication 필수)
  → AI가 최종 기사 선정 + 제목 재작성 + 시사점 작성 (섹션당 여러 건 유지 — 예전엔 "웹 화면에서
    가장 앞에 남은 1개만" 강제로 잘랐던 로직이 있었음, 지금은 관리자가 검토 화면에 남긴 개수 그대로 전부 반영)
  → AI 응답 JSON 정리(URL 검증 등 안전장치 포함, 내부 리서치 결과 원문 링크는
    raw_json.source_summaries[].url에서 가져오도록 필드 매핑 버그 수정)
  → 관리자 승인용 후보 초안 반환

[B] 관리자 승인 및 발송 예약 (Human-in-the-Loop)
  → 관리자가 "AI 초안 가져오기"를 누르면 곧바로 실제 발송 템플릿 모습 그대로 렌더링됨
    (예전에는 폼 입력창 목록 + 별도 "미리보기" 버튼으로 분리되어 있었음 → 지금은 하나의 화면으로 통합)
  → 각 기사에 ▲▼(순서 변경) · 편집 · 삭제 버튼이 붙어 있어 그 자리에서 바로 수정 가능,
    변경 즉시 미리보기에 반영됨(스크롤 위치도 유지)
  → 섹션 끝에는 "+ 항목 추가" 버튼으로 새 기사를 바로 추가 가능
  → People & Culture "종합 코멘트"도 같은 방식으로 인라인 편집
  → 원문 기사 링크를 누르면: 인스타그램/틱톡/X/Threads/Facebook/YouTube 같이 iframe 삽입을
    서버 차원에서 막는 도메인만 새 탭으로 열리고, 일반 뉴스 기사는 미리보기 화면 안에서 그대로
    열림(이탈 시 "돌아가기" 버튼이 나타나고, 누르면 이탈 직전 스크롤 위치로 복귀)
  → 뉴스레터 본문은 word-break:keep-all 적용으로 한글 단어가 줄 중간에 끊기지 않음
  → "발송하기" 클릭 → n8n Webhook(`newsletter/queue`)으로 최종 데이터 전송
  → n8n이 승인본을 검증 + HTML 렌더링 + `newsletters` 시트에 QUEUED 상태로 저장 + 예약 접수 응답

[C] 예약 발송 실행
  → 매주 월요일 08시 스케줄 트리거로 QUEUED 상태 뉴스레터를 Gmail로 실제 발송
  → (테스트용 경로 추가됨) 관리자 승인 웹훅 직후 1분 뒤 자동 발송되는 별도 분기도 함께 존재
    — 운영 시엔 이 분기만 제거/비활성화하면 원래의 "월요일 08시만 발송" 구조로 복귀
  → 발송 완료 후 상태를 SENT로 업데이트
```

---

## 4. 뉴스크롤링 1/2단계 — 리서치 허브 백엔드

```
[1단계] 사용자가 키워드 조합 검색
  → Naver+Serper 검색(~20건) → 회사 맥락(600자)으로 관련성 평가 → 프론트엔드에 반환

[2단계] 사용자가 결과에서 자료 선택 후 "인사이트 도출"
  → 선택 URL 본문 추출(Tavily) → 추출 성공 여부 실제로 확인(과거엔 항상 true였던 버그 수정)
  → 본부 맥락(808자) 읽기 → 심층 리포트 프롬프트
  → AI 리포트 생성(Executive Brief, 사실요약, 분석, 추천액션, 참고자료)
  → 참고자료는 AI 응답과 무관하게 원본 문서의 실제 제목·URL을 코드에서 항상 보장하도록 처리
```

*(이번 세션에서는 이 두 워크플로우 자체에 대한 변경은 없었음. 뉴스레터 1단계 파이프라인이 이 2단계의
"AI가 URL을 지어내지 않도록 실제 검색 결과와 대조" 안전장치 패턴을 그대로 참고해 적용했음.)*

---

## 5. 회사 맥락 문서 체계

~~두 개의 Google Docs 문서를 용도에 따라 다르게 연결한다.~~ → **2026-07-25 Supabase `context_docs` 테이블로 이관 완료**(7번 섹션 35번 항목). Google Docs는 더 이상 n8n에서 읽지 않고, 원본은 백업용으로만 남아있음.

실제 점검 결과 문서는 4종이 아니라 **서로 다른 내용 3종**이었다 — 뉴스크롤링 1단계의 `8-A`는 이름은 "회사 맥락"이지만 실제로는 웹훅 입력이 비어있을 때 쓰는 기본값이 본부 맥락(900자) 문서였고, 이 기본값이 실제로 오버라이드된 적이 없어(프론트엔드가 해당 파라미터를 보낸 적이 없음) 사실상 항상 본부 맥락(900자)을 읽고 있었다. 이관하면서 이 실제 동작을 그대로 고정했다.

| context_docs.id | 성격 | 길이 | 연결된 곳 |
|---|---|---|---|
| **company_context_2000** | 회사 전체 차원의 넓은 정체성/서비스 설명. "이게 우리랑 관련 있나" 판단용 | 2000자 | 뉴스레터 1단계 `0-C` |
| **org_context_808** | 센터·팀 단위 세부 조직/담당 직무. "그래서 누가 뭘 해야 하나" 판단용 (노드 이름은 "회사 맥락"이지만 실제 내용은 본부 맥락) | 808자 | 뉴스크롤링 2단계 `4-A` |
| **org_context_900** | 센터·팀 단위 세부 조직/담당 직무 (org_context_808과 거의 동일한 내용, 글자수만 다름) | 900자 | 뉴스레터 2단계 `A1`, 뉴스크롤링 1단계 `8-A`(고정값으로 이관) |

**본부 맥락 핵심 내용**: 사업 개요(BPO 도급+ESOP), 조직/담당 직무(GA센터·BW센터·POC센터·ESOP워크센터·ESOP에듀센터별 담당 업무), 고객사(삼성전자 DS부문 우선·DX 후순위, ESOP 입주사는 별개), 전략 방향(Pain Point 기반 신사업, AX 정렬), AI 판단 지시(어느 센터·직무와 연결되는지 판단하라).

문서 내용을 고치고 싶으면 이제 Google Docs가 아니라 Supabase `context_docs` 테이블의 해당 행(`content` 컬럼)을 Table Editor에서 직접 수정하면 된다.

---

## 6. Research On 리서치허브 (프론트엔드) 주요 구조

- **파일 구조(2026-07-25 분리 완료)**: 기존 단일 `index.html`(305KB)을 `index.html`(마크업) + `styles.css` + `js/*.js` 11개 파일(config/keywords-state/research-results/insight-report/newsletter-subscriber/newsletter-admin/search-history/account-settings/event-bindings/auth/admin)로 분리함. 일반 `<script src>` 태그(모듈 아님)로 순서대로 로드하므로 전역 스코프 공유 동작이 분리 전과 동일하고, `file://`로 직접 열어도 그대로 동작함. 자세한 내용은 7번 섹션 참고.
- **인증**: Supabase, `@etners.com` 도메인 제한(클라이언트 단). ~~서버 RLS 미확인~~ → 2026-07-25 RLS 전면 적용 완료(7번 섹션, 8번 섹션 참고).
- **사이드바**: 로고/아바타 대신 "OOO님 환영합니다" 텍스트 + 톱니바퀴(계정 설정) 아이콘. 로그아웃 버튼은 사이드바 하단에 flex 레이아웃으로 항상 보이도록 고정(순수 flex-column 구조 — `position:fixed` 오버레이 방식은 오히려 문제를 일으켜 되돌림). 메뉴 구성: 리서치 허브(대시보드/새 리서치 만들기/리서치 결과/AI 인사이트 리포트/저장 자료함/검색 히스토리), 뉴스레터 허브(뉴스레터/구독 신청), 관리자 메뉴(계정 관리/뉴스레터 관리/뉴스레터 구독 관리 — 관리자 권한 보유자만 노출)
- **모바일 반응형**: 840px 이하에서 사이드바가 슬라이드인 드로어(+백드롭)로 전환되고, 테이블 위주 UI(리서치 결과·인사이트 히스토리 등)는 카드형으로 전환됨. 데스크톱용(`...Table`)과 모바일용 컨테이너가 분리되어 있고 렌더링 함수가 두 곳 모두 채움
- **PC 전용**: 사이드바 접기/펼치기(아이콘만 남기고, 호버 시 body-level 툴팁으로 메뉴명 표시)
- **대시보드**: 1040px 폭으로 통일된 컨테이너 안에 브랜드 로고/검색창/바로가기 6개(카드형, 확대됨)/위젯 영역이 배치됨. 예전엔 위쪽(브랜드/검색)은 640px로 좁고 아래쪽 위젯 카드는 화면 전체 폭으로 늘어나는 불일치가 있었음 → 통일. **위젯 영역(2026-07-25 개편)**: 왼쪽 컬럼에 "최근 본 리서치"·"오늘의 추천 키워드" 카드를 세로로 쌓고, 오른쪽에는 그 둘을 합친 높이의 큰 카드로 "최근 클라이언트 동향" 캐러셀을 배치(7번 섹션 36번 항목 참고).
- **새 리서치 만들기**: 키워드 Pool(1/2/3-Depth) 탐색 → 전방 확장 → 후방 확장, 드래그 정렬 가능, 그룹별(전방/핵심/후방) 삽입 위치 규칙 적용. 고급 옵션에서 제외 사이트/키워드 직접 추가 가능(기존 프리셋과 도메인 정규화 비교 후 중복이면 자동 스킵)
- **AI 인사이트 리포트**: 목록(리스트) + 상세 페이지 분리 구조, Supabase에 누적 저장
- **뉴스레터 허브(구독자용)**: 구독 신청/해지 팝업(이메일 수정 가능), 다른 구성원 대리 구독 등록, 히스토리(발송본 iframe 미리보기, 한글 줄바꿈 방지 적용)
- **뉴스레터 관리(관리자 전용)**: 3번 섹션에 상술한 실시간 편집 미리보기 방식으로 전면 개편됨
- **계정 관리(관리자 전용)**: 계정 목록/권한 변경/비밀번호 초기화(n8n 웹훅 연동됨, 7번 섹션 19번 항목)/삭제, 새 계정 발급 모달
- **뉴스레터 구독 관리(관리자 전용)**: 구독자 목록/발송 상태/일시정지·해제/구독취소
- **저장 자료함**: 전체/기사/리포트 필터 탭 우측에 "삭제" 토글 버튼 → 활성화 시 각 자료 카드에 체크박스 노출(카드 클릭으로도 토글), 전체선택/선택한 자료 삭제/취소 바 노출. 기존 자료별 개별 삭제 버튼은 제거됨(2026-07-25).
- **지원 및 문의**: 사용자는 "계정 관련 문의"/"오류 신고" 카드의 "내역 확인" 버튼으로 본인이 제출한 내역과 관리자 답변을 모달로 확인 가능. 관리자는 사이드바 관리자 메뉴의 "지원 및 문의 관리" 페이지(문의 내역/오류 신고 탭)에서 목록 확인 + 답변 작성 + 상태(대기중/처리완료) 변경 가능(2026-07-25 신설).

### Supabase 테이블 변경사항 (담당자 확인 필요)

| 테이블/컬럼 | 용도 |
|---|---|
| `profiles.position` | 계정 설정의 "직위" 필드 |
| `profiles.newsletter_paused` | 관리자가 특정 구독자 수신을 일시정지할 때 사용 |
| `profiles.newsletter_last_send_status` | 마지막 발송 성공/실패 기록용 (컬럼만 준비, 값 채우는 로직은 아직 없음) |
| `support_requests` (신규 테이블) | 계정 설정 > 지원 및 문의(계정 문의/오류 신고) 제출 내용 저장. 2026-07-25에 `admin_reply`/`replied_at`/`replied_by` 컬럼 추가(관리자 답변 기능) + DELETE 정책 추가(본인 소유·미답변 건만 삭제 가능) |
| `newsletter_guest_subscribers` (신규 테이블) | Research On 계정이 없는 사람을 뉴스레터에 대리 등록할 때 사용 |
| `context_docs` (신규 테이블, 2026-07-25) | 회사/본부 맥락 문서 3종 (예전: Google Docs). id/label/content. 5번 섹션 참고 |
| `newsletter_keywords` (신규 테이블, 2026-07-25) | 뉴스레터 1단계 검색 Job 생성용 키워드 목록 (예전: `newsletter_keywords` 시트 탭) |
| `newsletter_candidates` (신규 테이블, 2026-07-25) | 뉴스레터 1단계가 쌓는 후보, 2단계가 조회 (예전: `newsletter_research` 시트 탭) |
| `research_reports` (신규 테이블, 2026-07-25) | 뉴스크롤링 2단계가 쌓는 리포트, 뉴스레터 2단계가 RESEARCH ON INSIGHT 후보로 조회 (예전: `research_results` 시트 탭). **사용자 개인 저장함인 `insight_reports`와는 별개** — 이쪽은 리포트가 생성될 때마다 전부 쌓이는 전체 피드 |
| `newsletters` (신규 테이블, 2026-07-25) | 뉴스레터 발송 큐/이력 (예전: `newsletters` 시트 탭) |
| `client_trend_articles` (신규 테이블, 2026-07-25) | 대시보드 "최근 클라이언트 동향" 캐러셀용. 매일 07시 n8n이 삼성전자 관련 최신 기사를 중복 제거 후 최대 5건으로 갈아끼움. 로그인 사용자는 읽기만 가능(RLS), 쓰기는 n8n service_role 전용. `image_url` 컬럼은 있지만 현재 프론트 UI에서는 안 씀(썸네일을 뺐음 — 7번 섹션 38번 항목) |
| `find_masked_user_id(name, email)` RPC (신규, 2026-07-26) | 로그인 화면 "아이디 찾기"용. 성명+이메일 일치 시 아이디를 마스킹(앞2·뒤2만 노출)해서 반환. `anon` 실행 권한 부여(로그인 전에도 호출 가능) |

> 위 5개 신규 테이블은 n8n만 접근하는 백엔드 전용 테이블 — 프론트엔드는 직접 조회/수정하지 않으므로 RLS는 켜두되 anon/authenticated 권한은 아예 주지 않았음(n8n은 secret 키로 RLS를 우회). 정책 SQL은 `supabase/migrations/20260725020000_n8n_sheets_docs_to_supabase.sql` 참고.

> ~~RLS(Row Level Security) 정책 미확인 상태~~ → **2026-07-25 RLS 전면 적용 완료**(7번 섹션 새 항목 참고). `profiles` 등 9개 테이블이 RLS 없이 `anon`(비로그인) 롤에 풀 CRUD 권한이 열려있던 심각한 취약점을 발견해 전부 잠갔음. `is_admin()` security-definer 함수로 "본인 행 또는 관리자만" 접근 가능하도록 정책을 설계했고, RLS 때문에 막히는 소수의 "타인 데이터 조회/수정" 흐름(로그인, 회원가입 중복확인, 뉴스레터 타인 대신 구독)은 좁은 권한의 RPC로 대체함. SQL은 `supabase/migrations/`에 기록. Supabase Management API 접근용 Personal Access Token은 로컬 `~/.supabase_pat`에 보관(git 제외).

---

## 7. 지금까지 발견하고 수정한 주요 이슈 (연대기)

1. Groq → Gemini 전환, JSON 파싱 안정화(2-pass 복구 로직)
2. PEOPLE_CULTURE 섹션 1건 제한 버그 (`slice(0,1)` 하드코딩 → `maxItems` 파라미터화)
3. 소스 URL 할루시네이션 방지 — AI가 지어낸 URL을 실제 검색 결과 URL 집합과 대조해 제거
4. 인사이트 리포트 참고자료 완전 공백 → 원인은 AI 응답을 그대로 신뢰한 것; **원본 문서(title/url)를 코드 레벨에서 항상 보장**하는 방식으로 근본 해결
5. 뉴스크롤링 2단계 `success` 플래그가 무조건 `true`였던 버그 — 실제 추출 성공 여부(`documents.length > 0`)로 수정
6. Gmail 검색 필터 문법 오류(콤마 대신 `OR` 사용해야 함), 검색 필터가 예전 개인 테스트 이메일로 남아있던 실수 발견·정정
7. 6월 기사(노란봉투법)가 "최근 7일" 필터를 통과해 들어온 문제 — 검색엔진 자체 날짜 필터는 신뢰할 수 없어, 발행일 10일 초과 시 코드에서 하드 컷 추가
8. PEOPLE_CULTURE 시사점이 너무 일반론적(구체적 사례 누락) → "고유명사 최소 1개 포함 필수" 지시로 강화(1단계·2단계 양쪽)
9. 메일 본문 캡(4000자)이 캐릿처럼 소수의 트렌드를 깊게 다루는 뉴스레터의 뒷부분을 통째로 날려버림 → 청크 분할 방식으로 전환(6000자 단위), 관리성 메일 필터링 추가
10. 광고/협찬 블록 제거 정규식이 실제로는 매칭 실패하고 있었음(추적용 URL이 너무 길어 종료조건을 못 채움) → 종료조건에 CTA 문구(👉) 인식 추가해 수정
11. CLIENT_WATCH 3건, BUSINESS_WORK 0~1건으로 너무 적게 선정됨 — 원인은 (a) 전체 후보(52개) 중 섹션당 10개만 AI에게 노출되던 것 (16개로 상향), (b) "최대 6개 골라라"는 열린 질문 자체가 AI를 보수적으로 만드는 경향

**(이하 이번 세션에서 새로 발견·수정)**

12. 사이드바 로그아웃 버튼이 화면에 안 보이는 문제 — 여러 차례 재발. 최종적으로 실제 렌더링 엔진(wkhtmltoimage)으로 직접 스크린샷 찍어가며 검증한 결과, `position:fixed` 오버레이 방식이 오히려 컨테이닝 블록 문제를 일으킬 소지가 있었고, 검증된 순수 flex-column 방식(관리자 메뉴 포함 최악의 케이스 + 작은 화면에서도 확인)으로 되돌려 해결
13. 대시보드 레이아웃 개편 — 위/아래 폭 불일치 해소(1040px 통일), 바로가기 아이콘 카드형으로 확대, 검색창/키워드 칩 확대
14. 뉴스레터 원문 링크 여러 건:
    - 내부 리서치(RESEARCH ON INSIGHT) 결과에 원문 링크가 안 붙던 문제 — `raw.source_urls`가 아니라 실제로는 `raw.source_summaries[].url`에 저장되어 있었음(필드명 불일치)
    - People & Culture 섹션에 원문 링크가 거의 안 붙던 문제 — 검색 자체는 되고 있었으나 (a) search_query가 영문 그대로라 한국 뉴스 검색에서 결과 없음, (b) Naver/Serper가 단어 단위로 느슨하게 매칭해서 무관한 기사가 섞임, (c) 코드 노드 하나(`3-C-6-1`)에 n8n의 "항목별 실행" 모드 설정이 빠져서 8개 후보 중 1개만 살아남던 심각한 버그 — 세 가지 다 순차적으로 발견·수정
    - 관리자 미리보기에서 원문 링크를 누르면 iframe이 그 자리에서 인스타그램 등으로 이동을 시도하다가 차단당하는 문제 → SNS류 도메인만 새 탭, 나머지는 기존처럼 iframe 내 이동으로 절충
15. **People & Culture 섹션에 조직 운영 시사점이 아예 없던 문제** — 실제 발송본을 CL4 이상 보직자 눈높이로 검토하다가 발견. 원인은 파이프라인 3곳(1단계 프롬프트, 2단계 편집 프롬프트, 2단계 렌더링 코드)에서 전부 "People & Culture는 시사점 비워둬라/생략해라"고 명시하고 있었던 예전 설계. 세 곳 전부 뒤집어서 항목별 조직 운영 시사점(채용·유지·조직문화·세대소통·복리후생 관점)을 필수화
16. CL4+ 보직자 대상으로 전체 섹션(01~03)의 요약·시사점이 너무 짧고 상투적이었던 문제 — 요약은 2~3문장 + 구체적 수치/시점 포함, 시사점은 "무엇을 검토·논의해야 하는지"까지 담도록 프롬프트 강화 (1단계 평가 프롬프트, 2단계 편집 프롬프트 양쪽)
17. 뉴스레터 관리(HITL) 화면 UX 전면 개편 — 폼 입력창 목록 + 별도 미리보기 버튼 구조에서, "AI 초안 가져오기"를 누르면 바로 실제 발송 템플릿 모습으로 표시되고 그 화면 안에서 편집·삭제·순서변경(▲▼)·항목추가가 실시간 반영되는 구조로 전환
18. 관리자가 모바일 반응형 최적화를 별도로 작업한 `index.html`을 새 기준본으로 채택 — 기존 세션의 작업물(로그아웃 버튼, 대시보드 레이아웃 등)이 모두 유지된 상태에서 병합되었음을 확인 후 진행
19. HTML 내 n8n 미연동 지점 전수 점검(모든 `fetch()` 호출부 + "TODO"/"미구현" 문구 검색) 후, 실제로 웹훅이 없던 관리자 전용 기능 2건에 대해 신규 n8n 워크플로우를 제작하고 production 웹훅으로 연결:
    - **계정 관리 &gt; 비밀번호 초기화**: 새 워크플로우 `[Research On] 관리자 - 계정 비밀번호 초기화` 생성. Supabase Auth Admin API(`PUT /auth/v1/admin/users/{id}`)를 호출해 `profiles.id`(=auth.users.id) 기준으로 비밀번호를 기존 안내 문구와 동일한 기본값(`dlxmsjtm1!`)으로 초기화. Supabase가 새 API 키 체계(`sb_publishable_...`)를 쓰고 있어 관리자 API 호출도 `apikey` 헤더 하나만 필요(Authorization Bearer 이중 헤더 불필요)함을 Supabase 공식 문서로 확인 후 반영. service_role 키는 프론트엔드에 절대 노출하지 않고 n8n의 HTTP Header Auth 크리덴셜로만 보관.
    - **뉴스레터 구독 관리 &gt; 개별 구독자 "뉴스레터 발송"(강제 발송)**: 기존 `[Research On] 뉴스레터 - 2단계` 워크플로우에 D0~D6 분기를 추가(같은 Google Sheets/Gmail 크리덴셜 재사용). `newsletters` 시트에서 `SENT` 상태 중 최신 호(없으면 `QUEUED` 중 최신 호)를 골라 지정한 구독자 1인에게 즉시 재발송.
    - 두 웹훅 모두 프론트엔드와 동일한 문자열을 대조하는 최소 방어용 `admin_token`(`ADMIN_ACTION_TOKEN`)을 요청 바디에 함께 실어 보내도록 처리 — 다만 이 값은 프론트 JS에 그대로 노출되므로 완전한 인증 수단은 아니며, 진짜 보호가 필요하면 호출자의 Supabase 세션(JWT)을 서버 단에서 검증하는 절차가 추가로 필요함(8번 섹션에 미해결 항목으로 기록).
20. **AI 인사이트 리포트에 "내보내기(PDF/Word)" 추가** — 리서치 결과 화면에 이미 있던 내보내기와 유사한 방식으로 구현. 리포트 상세 화면 헤더에 "⬇ 내보내기 ▾" 드롭다운을 추가(리서치 결과와 동일한 `.export-menu-item` 스타일 재사용).
    - PDF: html2canvas + jsPDF를 동적 로드해 오프스크린 div를 캔버스로 렌더링 후 세로 A4로 페이지 분할 저장(리서치 결과는 표 위주라 가로 A4를 쓰지만, 리포트는 서술형이라 세로 A4로 함). 이때 사용하는 렌더러 `reportTabsPrintHtml()`은 신규 작성 — 기존 화면 렌더러 `reportTabsHtml()`이 쓰는 `var(--x)` CSS 변수를 html2canvas가 안정적으로 읽지 못할 수 있어(리서치 결과 PDF 내보내기도 처음부터 같은 이유로 고정 hex 컬러를 써왔음), 색상을 전부 고정값으로 옮겨 새로 작성함.
    - Word: 최초 구현 시 이미 작성돼 있었지만 어디서도 호출되지 않던 죽은 코드 `reportTabsWordHtml()`을 재사용해 "HTML을 `.doc` 확장자로 저장"하는 방식으로 연결했었으나, **21번 항목에서 실제 기기 테스트 결과 파일이 열리지 않는 문제가 발견되어 이후 완전히 교체됨**(아래 참고).
    - 리포트 데이터 구조(`report.tabs` = `executive_brief` / `sections` / `sources` 3가지 type)는 n8n [뉴스 크롤링 2단계] 워크플로우의 "8. Code - AI 응답 JSON 정리" 노드가 이미 확정해서 내려주고 있어, 프론트 3개 렌더러(화면용/PDF용/Word용) 모두 같은 스키마를 공유함 — n8n 쪽 변경 없음.
21. **PDF 잘림 버그 + Word 파일이 안 열리는 문제 수정, 뉴스레터 신규 구독 웰컴 메일 추가** (사용자가 실제 기기에서 다운로드해보다가 발견)
    - **PDF 잘림 버그**: AI 인사이트 리포트처럼 세로로 긴 콘텐츠를 PDF로 내보내면 특정 지점(스크린샷 기준 "리스크" 섹션)에서 잘리고 페이지도 1장으로만 나오는 문제. 원인은 페이지 분할(`sliceH`) 계산식이 실제 페이지 높이(mm)와 무관한 엉뚱한 공식(`canvas.width/ratio * (pageH/pageW) * 2`)이었던 것 — 세로로 긴 콘텐츠에서는 이 값이 지나치게 커져 "전체 내용이 한 페이지에 다 들어간다"고 오판하고, 실제 페이지 경계를 넘는 부분은 그대로 잘려서 안 보이게 됨. `pxToMm = pageW/canvas.width`, `sliceH = pageH/pxToMm`로 페이지 실치수 기준으로 정확히 재계산하도록 수정(리서치 결과 PDF 내보내기도 동일한 버그가 있어 같이 수정).
    - **Word 파일이 안 열리는 문제**: 원인은 애초 설계 자체의 한계 — "HTML 문서를 `.doc` 확장자로 저장"하는 오래된 트릭은 데스크톱 MS Word는 열어주지만 모바일 Word 앱·구글독스·한컴오피스 등 다른 프로그램은 손상된 파일로 인식해 열지 못함. `docx`(dolanmiu/docx, CDN: `cdn.jsdelivr.net/npm/docx@8.5.0/build/index.umd.js`) 라이브러리로 교체해 실제 유효한 OOXML(.docx) 바이너리를 직접 생성하도록 리포트·리서치 결과 Word 내보내기를 모두 재작성함(Node.js에 동일 라이브러리를 설치해 실제 코드를 그대로 실행 → 생성된 .docx를 LibreOffice로 직접 열어서 정상 변환되는지까지 검증 후 반영). 더 이상 쓰이지 않는 `reportTabsWordHtml()`은 제거.
    - **뉴스레터 신규 구독 웰컴 메일**: '구독 신청' 페이지에서 본인 구독 또는 타인 대리 등록(기존 계정/외부 게스트 양쪽 다)이 성공하면 새 웹훅(`newsletter/welcome`)을 호출하도록 프론트엔드 3곳에 연결. 신규 워크플로우 `[Research On] 뉴스레터 - 신규 구독 웰컴 메일 및 최신호 즉시 발송`을 제작 — 트리거 한 번이 두 갈래로 동시에 나뉘는 fan-out 구조(뉴스레터 2단계의 "승인 후 1분 대기" 분기와 동일한 패턴)로, (A) 웰컴 메일을 바로 보내고 즉시 웹훅 응답을 반환, (B) 이와 별개로 5분을 기다렸다가 `newsletters` 시트에서 가장 최근 SENT(없으면 QUEUED) 호를 찾아 그 구독자에게 바로 발송. 웰컴 메일 문구는 참고용으로 첨부하겠다던 타 뉴스레터 웰컴 메일 파일이 실제로는 첨부되지 않아 직접 새로 작성함(매주 월요일 오전 8시 정기발송 안내 포함) — 추후 참고 파일을 받으면 톤/포맷을 맞춰 다시 다듬을 수 있음.
22. **계정 관리 &gt; 새 계정 발급 모달에 부서/직위 필드 추가** — 기존에는 아이디/이름/이메일/역할/초기 비밀번호만 입력 가능했음. `profiles` insert 코드 자체는 이미 `affiliation`/`position` 컬럼을 받고 있었지만 항상 빈 문자열로 고정돼 있었던 것을 확인 — 계정 설정 페이지에 있던 것과 동일한 부서(조직도)/직위 드롭다운을 그대로 재사용해 입력 UI만 새로 추가함.
23. **뉴스레터 신규 구독 시 `permission denied for table newsletter_guest_subscribers` 오류** — 사용자가 실제로 대리 구독 등록을 시도하다가 발견. RLS 정책 위반이 아니라 그보다 근본적인 테이블 자체의 GRANT 권한 누락(신규 테이블이라 아직 권한 설정이 안 된 상태)으로 진단 — Supabase SQL Editor에서 실행할 `GRANT`/정책 SQL을 안내함. ~~실제 실행 여부는 미확인~~ → 2026-07-25 확인 결과 이미 정상 반영되어 있었음(아래 28번 항목 RLS 점검 중 확인).
24. **사이드바 로그아웃 버튼이 다시 안 보이는 문제** (12번 항목 이후 재발) — 사용자가 예전에 로그아웃 버튼이 살아있던 별도 HTML 파일(`index3_logout_flex_bottom.html`)을 첨부하며 비교를 요청. 헤드리스 브라우저(Playwright)로 관리자 메뉴까지 포함한 최악의 케이스를 실제 좌표까지 측정해 비교한 결과, 현재 작업본의 레이아웃 구조 자체는 문제가 없었음 — 진짜 원인은 `.sidebar`가 `height:100vh`만 쓰고 있던 것. 모바일 브라우저에서 `100vh`는 주소창/툴바 뒤에 가려진 영역까지 포함해 실제 화면보다 커질 수 있어, 사이드바 맨 아래 요소(로그아웃 버튼)가 화면 밖으로 밀려나는 전형적인 버그였음. `height:100vh; height:100dvh;`로 폴백을 추가해 해결. (참고로 비교차 첨부받은 파일 쪽은 버튼 id가 `logoutDisplayBtn`으로 바뀌어 있는데 클릭 이벤트는 여전히 `logoutBtn`을 찾고 있어, 그 파일 자체는 버튼이 눌러도 반응이 없는 상태였음 — 현재 작업본은 이런 id 불일치가 없음을 확인함.)
25. **리서치 결과 상세 목록 UX 개선 + AI 인사이트 리포트 PDF 섹션 잘림 방지**
    - 목록의 각 항목을 체크박스를 정확히 눌러야만 선택할 수 있던 것을, 항목 전체(체크박스/버튼 제외 영역) 클릭으로 토글되게 변경. 제목을 눌러 상세 팝업을 열던 방식은 없애고, "저장" 왼쪽에 "상세보기" 버튼을 별도로 추가.
    - PDF로 내보낼 때 "기회 요인", "리스크" 같은 소제목 단위 섹션이 페이지 경계 중간에서 잘리던 문제 — `reportTabsPrintHtml()`이 만드는 각 소제목 블록에 `print-block` 마커를 붙이고, PDF 페이지 분할 시 그 블록 좌표를 미리 재서 경계가 블록 중간을 지나가면 블록 시작점에서 페이지를 끊도록 페이지 분할 로직을 재작성. html2canvas/jsPDF를 로컬에 내려받아 실제 코드를 헤드리스 브라우저에서 그대로 실행한 뒤, 생성된 PDF의 실제 페이지 분할 좌표를 블록 좌표와 대조해 섹션이 안 잘림을 수치로 검증함.
26. **회원가입 폼에 부서(전체 조직도)/직위 필드 추가 + 부서 드롭다운 표기 정리** — 회원가입 화면의 "소속"이 4개짜리 약식 목록(BW센터 1팀 등)이었던 것을 계정 관리 쪽과 동일한 전체 조직도 드롭다운으로 교체하고, "직위" 필드를 신규 추가. 이 과정에서 기존 부서 드롭다운(계정 설정/새 계정 발급/회원가입 3곳 모두)에 쓰이던 `-`, `.`, `*` 기호가 실제 UI 표기용이 아니라 계층 구조를 설명하려고 편의상 쓴 것이었음이 확인돼, 전각 공백만으로 들여쓰기하는 방식으로 3곳 다 정리. `handleSignup()`도 직위 값을 `profiles`에 저장하도록 반영.
27. **부서 선택 후 상위 조직이 안 보이는 문제** — 드롭다운에서 "1팀"을 선택하면 닫힌 select 박스에도 "1팀"만 표시돼 어느 센터 소속인지 알 수 없던 문제. select의 `value`에는 원래도 전체 경로(`BPO사업본부 &gt; DS지원사업부 &gt; GA센터 &gt; 1팀`)가 들어있었으므로, 목록 자체(들여쓰기된 짧은 이름)는 그대로 두고 select 아래에 선택된 전체 경로를 보여주는 작은 미리보기 텍스트를 추가(계정 설정/새 계정 발급/회원가입 3곳 모두, 공용 함수 `updateDeptPathPreview()`).

**(이하 2026-07-24~25 세션에서 새로 진행)**

28. **GitHub 저장소 실시간 연동** — 로컬 프로젝트 폴더에 git을 처음 연결함. 기존 GitHub 저장소(`bpodsplan1/Research-On`, 지금까지 웹 UI 드래그 업로드로만 관리되던 곳)를 그대로 `origin`으로 붙이고, 원격의 실제 커밋 히스토리 위에 로컬 최신 작업분을 얹는 방식으로 연결(`git reset origin/main` 후 재커밋 — 히스토리 보존). GitHub 인증은 이 기기 전용 SSH 키(`~/.ssh/id_ed25519_github`)를 새로 만들어 사용자 GitHub 계정에 공개키를 등록하는 방식으로 설정. 작업 원칙: 커밋은 매 작업 단위로 자동으로 남기되, 실제 `push`는 사용자가 명시적으로 요청할 때만 실행.
29. **단일 `index.html`(305KB) → `index.html` + `styles.css` + `js/*.js` 11개 파일로 분리** — 코드 자체는 한 글자도 바꾸지 않고 순수하게 파일만 나눔. 기존 코드의 섹션 배너 주석(`// ══...`) 지점을 경계로 삼아 각 파일이 독립적으로 문법 완결되도록 나눴고, 분리 전/후를 diff로 대조해 바이트 단위로 무손실임을 검증. 일반 `<script src>` 태그(모듈 아님)라 전역 스코프 공유가 그대로 유지되고 `file://` 직접 열기도 계속 가능함. `index.html`은 305KB → 62KB로 가벼워짐.
30. **저장 자료함에 다중 선택 삭제 기능 추가** — 기존 자료별 개별 삭제 버튼 제거, 필터 탭 우측에 "삭제" 토글 버튼 신설. 활성화 시 카드 우측 상단 체크박스 노출(카드 클릭으로도 토글, 리서치 결과 상세 목록과 동일한 패턴 재사용) + 전체선택/선택삭제/취소 바 노출.
31. **Supabase RLS(Row Level Security) 전면 보안 잠금** — 사용자가 Supabase Management API 접근을 허용해줘서(Personal Access Token, 로컬 `~/.supabase_pat`) 실제 DB 권한 구조를 점검한 결과, `profiles`를 포함한 11개 테이블 중 9개가 **RLS가 아예 꺼진 채로 `anon`(비로그인) 롤에 SELECT/INSERT/UPDATE/DELETE 풀 권한이 열려 있는** 심각한 취약점을 발견함. `index.html`의 Supabase URL+publishable key가 이미 공개돼 있어, 로그인 없이 누구나 전체 회원정보 열람·`role`을 admin으로 변조·타인의 저장자료/검색기록 삭제가 가능한 상태였음.
    - `is_admin()` security-definer 헬퍼 함수로 "본인 행 또는 관리자만" 접근 가능한 RLS 정책을 9개 테이블 전부에 설계·적용(`research_history`/`saved_docs`/`search_history`/`subscribed_keywords`/`keyword_sets`/`insight_reports`/`news_summary_history`는 본인 행만 CRUD, `profiles`/`admin_requests`는 본인 또는 관리자, `newsletter_send_history`는 로그인 사용자 읽기 전용). `profiles.role` 자가 변경을 막는 트리거도 추가.
    - `support_requests`는 RLS는 켜져 있었지만 GRANT 자체가 빠져있어 "지원 및 문의" 제출 기능이 완전히 막혀 있던 것도 함께 발견·수정. `newsletter_guest_subscribers`는 이미 정상 상태(23번 항목의 예전 수정이 실제로 적용돼 있었음)로 확인.
    - RLS 적용으로 기존에 "로그인 전(anon)에 profiles를 직접 조회"하거나 "로그인 사용자가 타인의 profiles 행을 직접 update"하던 프론트 로직 몇 곳이 새로 막히게 됨을 미리 찾아내 좁은 권한의 RPC로 교체: 로그인 화면 아이디→이메일 조회(`get_email_by_user_id`), 회원가입 중복확인(`check_user_id_available`/`check_email_available`), 뉴스레터 "타인 대신 구독 신청"의 대상 조회·구독처리(`find_profile_by_email`/`subscribe_profile_newsletter`). **다만 로그인 화면 쪽 RPC 하나(`get_email_by_user_id`)를 처음에 놓쳐서 RLS 적용 직후 잠깐 전체 로그인이 막히는 사고가 있었음** — 발견 즉시 사용자가 Supabase SQL Editor에서 직접 SQL을 실행해 복구.
    - 관리자/일반사용자/비로그인 세 시나리오를 `request.jwt.claims`로 실제 시뮬레이션(SQL)해 의도대로 동작함을 전부 검증. SQL은 `supabase/migrations/`에 기록.
    - Supabase Management API로 DDL을 실행할 때 Claude Code 자동모드 분류기가 매번 개별 승인을 요구함 — 급할 땐 Supabase 대시보드 SQL Editor에서 사용자가 직접 실행하는 우회로를 씀.
32. **지원 및 문의: 사용자 내역 확인 + 관리자 답변 기능 신설** — `support_requests`에 `admin_reply`/`replied_at`/`replied_by` 컬럼과 관리자 전용 UPDATE 정책 추가. 사용자 쪽엔 계정 설정의 "계정 관련 문의"/"오류 신고" 카드에 "내역 확인" 버튼을 달아 본인 제출 내역+관리자 답변을 모달로 확인 가능하게 함. 관리자 쪽엔 사이드바 관리자 메뉴에 "지원 및 문의 관리" 페이지를 신설(문의 내역/오류 신고 탭, 카드 펼쳐서 답변 작성+상태 변경).
33. **n8n [뉴스레터 2단계]에 발송상태 갱신 노드 추가** — 관리자 구독자 관리 화면에 있던 "발송성공/실패" 배지가 그 값을 채우는 로직이 없어 항상 비어 있던 문제. 실제 발송 대상 `bpo.dsplan1@gmail.com`이 (구독자 전원에게 자동 전달되는) Google Group임을 확인한 뒤, 기존 노드는 하나도 건드리지 않고 발송 성공 직후(C5 뒤) 새 노드 `C5B. HTTP Request`만 추가해 구독 중인 전체 `profiles` 행의 `newsletter_last_send_status`를 `success`로 일괄 갱신하도록 함. 실패 케이스는 기존 워크플로우도 원래 별도 처리가 없어(Gmail 실패 시 워크플로우 자체가 중단됨) 이번 범위에서는 다루지 않음 — 손대면 오히려 기존 Google Sheets 상태 기록 로직까지 건드려야 해서 리스크가 크다고 판단.
34. **저장 키워드 세트(`keyword_sets`) 저장 기능은 보류 확정** — 점검 중 저장 UI/로직 자체가 아예 없다는 걸(조회·삭제 함수만 있고 어디서도 호출되지 않는 죽은 코드) 발견해 신규로 설계하려다, 사용자가 "지금으로 충분, 딱히 필요 없을듯"이라고 확인해 보류. `js/keywords-state.js`의 `searchKeywordSet`/`deleteKeywordSet`는 여전히 죽은 코드로 남아있음.

**(이하 2026-07-25 세션에서 새로 진행 — n8n이 쓰던 Google Sheets/Docs를 전부 Supabase로 이관)**

35. **n8n 5개 워크플로우가 쓰던 Google Sheets 4탭 + Google Docs 맥락 문서 3종을 Supabase로 전면 이관** — 사용자가 "Google Sheets/Docs 데이터를 모두 Supabase로 바꾸고 싶다"고 요청. Google Drive 조회(MCP)로 실제 시트 컬럼/문서 내용을 먼저 확인한 뒤 설계.
    - **신규 Supabase 테이블 5개**: `context_docs`(맥락 문서 3종), `newsletter_keywords`, `newsletter_candidates`(구 newsletter_research), `research_reports`(구 research_results — 사용자 개인 저장함 `insight_reports`와는 별개 테이블), `newsletters`(발송 큐/이력). 전부 n8n 전용 백엔드 테이블이라 RLS는 켜고 anon/authenticated 권한은 주지 않음(SQL: `supabase/migrations/20260725020000_n8n_sheets_docs_to_supabase.sql`).
    - **기존 데이터까지 이관**(사용자가 히스토리 보존을 원함). 맥락 문서 3종은 Drive에서 읽은 내용을 그대로 SQL INSERT. Google Sheets 4탭(newsletter_keywords/newsletter_research/research_results/newsletters)은 Drive 조회 API가 스프레드시트의 첫 번째 탭만 내보내는 한계가 있어, 대신 **1회성 이관 전용 n8n 워크플로우**(`[Research On] 1회성 - Sheets 데이터 Supabase 이관.json`)를 새로 만듦 — 기존 프로덕션 워크플로우들이 이미 쓰고 있던 Google Sheets OAuth 크리덴셜을 그대로 재사용해 4탭을 읽고 Supabase로 POST(upsert)한다. **사용자가 n8n에 이 파일을 가져와 한 번 실행한 뒤 삭제하면 됨** — 8번 섹션에 미완료 항목으로 기록.
    - **5개 프로덕션 워크플로우 수정**(뉴스레터 1/2단계, 뉴스크롤링 1/2단계, 웰컴메일): 각 Google Sheets/Docs 노드를 Supabase REST API 호출(HTTP Request, 기존 `C5B` 노드와 동일하게 `Supabase` HTTP Header Auth 크리덴셜 재사용)로 교체. **노드 이름·id·position은 그대로 유지**하고 타입/파라미터/크리덴셜만 바꿔서, 다운스트림 Code 노드가 `$('노드이름')`으로 참조하는 부분을 거의 손대지 않아도 되게 설계함.
    - Code 노드 쪽 최소 수정: (a) `raw_json`/`newsletter_json` 필드를 `JSON.stringify()`한 문자열로 Sheets 텍스트 컬럼에 넣던 부분을, Supabase `jsonb` 컬럼에 맞게 객체 그대로 보내도록 수정(뉴스레터 1단계 3곳, 뉴스크롤링 2단계 1곳, 뉴스레터 2단계 2곳: `B2`/`B-T0`), (b) 뉴스레터 2단계 `A4`의 `getDocText()`가 Google Docs의 중첩 `textRun` 구조만 파싱할 수 있었던 것을, Supabase가 돌려주는 평문 `content` 문자열도 바로 읽도록 fast-path 추가.
    - 뉴스크롤링 1단계 `8-A`는 원래 웹훅 입력으로 문서 URL을 바꿀 수 있게 설계돼 있었지만 실제로는 한 번도 오버라이드된 적이 없어(프론트엔드가 해당 파라미터를 보낸 적 없음) 항상 같은 기본값 문서(본부맥락 900자)를 읽고 있었던 것을 확인 — 이관하면서 이 실제 동작 그대로 고정(`org_context_900` 고정 조회).
    - Google Sheets/Docs 원본은 전혀 손대지 않고 그대로 둠(백업용). 실제 n8n 반영은 이전 세션과 동일하게, 이번에 수정한 JSON 파일들을 사용자가 n8n으로 재가져오기 해야 적용됨 — 아직 미착수(8번 섹션 참고).
    - **이관 후 실사용 검증 중 발견·수정한 이슈 3건**: (a) `newsletter_keywords`가 `id`(uuid)만 기본키라 `on_conflict=keyword,section` 업서트가 "no unique constraint" 에러로 실패 — 애초에 1회성 빈 테이블 채우기라 업서트가 불필요했으므로 단순 INSERT로 변경. (b) **이 Supabase 프로젝트의 `service_role`(secret 키)이 `public` 스키마 전체(신규 테이블뿐 아니라 `profiles` 등 기존 테이블도 전부)에 SELECT/INSERT/UPDATE/DELETE 기본 권한이 애초에 없었던 것을 발견** — `rolbypassrls=true`(RLS 우회)는 있었지만 그것과 별개로 테이블 자체의 GRANT 권한이 없어 n8n이 secret 키로 어떤 테이블도 쓸 수 없는 상태였음(즉 C5B 노드도 지금까지 한 번도 실제로는 성공한 적이 없었을 것). `supabase/migrations/20260725030000_grant_service_role_crud.sql`로 `public` 스키마 전체에 대해 GRANT + 앞으로 생성될 테이블에도 자동 적용되는 default privileges를 함께 설정해 해결. (c) `newsletters`/`research_reports`에 저장하는 Code 노드 결과물에 실제 테이블 컬럼에 없는 여분 필드(`recipient`/`scheduled_for`/`selected_count`/`warnings`, `original_response`)가 섞여 있어 `JSON.stringify($json)` 통짜 전송 시 PostgREST가 "Bad request"로 거부 — 예전 Google Sheets 자동매핑은 여분 필드를 조용히 무시했지만 HTTP Request는 그렇지 않다는 게 원인. `B3`/`8-B` 두 노드를 실제 테이블 컬럼만 명시적으로 골라 보내도록 수정.
36. **대시보드에 "최근 클라이언트 동향" 캐러셀 위젯 신설 + 사이드바 가로 스크롤 버그 수정** — 사용자가 실제 화면을 보며 요청.
    - **사이드바 가로 스크롤 버그**: `.sidebar>nav`가 `overflow-x:visible`이었고 `.nav-label`에 폭 제한이 전혀 없어서, "관리자 권한 신청"처럼 긴 메뉴 라벨이 사이드바 폭을 넘어가며 가로 스크롤을 유발하고 있었음. `overflow-x:hidden` + `.nav-item{min-width:0}` + `.nav-label{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`로 해결.
    - **대시보드 레이아웃**: "최근 본 리서치"/"오늘의 추천 키워드" 두 카드를 왼쪽 컬럼에 세로로 쌓고(`.dash-widget-col`), 오른쪽에는 그 합친 높이만큼의 큰 카드로 신규 캐러셀을 배치.
    - **신규 Supabase 테이블 `client_trend_articles`**: id/title/url/source/published_at/summary/created_at. 로그인 사용자는 읽기 전용(RLS `auth.role()='authenticated'` + `GRANT SELECT`), 쓰기는 n8n service_role 전용.
    - **신규 n8n 워크플로우 `[Research On] 클라이언트 동향 - 삼성전자 일간 수집`**: 매일 07시 Schedule Trigger → 기존 파이프라인과 동일한 Naver+Serper 크리덴셜로 "삼성전자" 뉴스 검색(Naver `sort=date`, Serper `tbs=qdr:d`로 최근 24시간 우선) → URL 정규화 + 제목 유사도로 중복 제거 → 발행일 최신순 상위 5건 → 매 실행마다 `client_trend_articles` 전체 삭제 후 5건 새로 저장(항상 5건 이하로 유지되는 갈아끼우기 방식). 아직 n8n에 가져오기·크리덴셜 연결·활성화 전(8번 섹션 참고).
    - **프론트엔드 캐러셀**: `js/keywords-state.js`에 `loadTrendCarousel()`(Supabase 조회+렌더) 및 `showTrendSlide`/`trendNext`/`trendPrev`/`startTrendAutoplay`/`stopTrendAutoplay` 추가, `js/event-bindings.js`에 좌우 버튼·점(dot) 클릭·마우스 오버 정지 이벤트 바인딩. 10초마다 자동 전환되고 캐러셀 위에 마우스를 올리면 멈췄다가 벗어나면 재개되는, 일반적인 포털 사이트 배너 캐러셀과 동일한 동작.
    - **(같은 세션 내 보완) 중복 판단 고도화 + 주식 시황 기사 하드 제외**: 실제 실행 결과를 사용자가 확인해보니, "삼성전자-브로드컴 2000억달러 협력"을 8개 매체가 각자 다른 제목으로 보도한 게 전부 별개 기사로 잡히는 문제와, 단순 주가 등락을 다루는 기사(예: "5주 연속 약세", "패닉")가 섞여 나오는 문제가 발견됨.
      - **주식 시황 제외**: 제목에 `주가/주식/증시/코스피/코스닥/상한가/하한가/급등/급락` 등 키워드가 있으면 하드 제외(단, "투자"는 사업 협력 기사에도 쓰이는 단어라 제외 목록에서 뺌). 이건 코드 레벨 키워드 필터로 충분히 신뢰할 수 있어 그대로 유지.
      - **동일 사건 판단은 결국 AI 판단으로 교체**: 처음엔 뉴스레터 1단계의 "동일 사건 클러스터링"(제목·요약 Jaccard 유사도 + 고유명사 공유 개수) 로직을 그대로 이식했으나, 두 차례 튜닝에도 위 브로드컴 사례를 계속 별개 기사로 오판(매체마다 "2000억 달러"/"292조원"/"290조원"처럼 숫자 표기가 다르고, 6~10단어 안팎의 짧은 한국어 헤드라인이라 겹치는 단어 자체가 적음). 문자열 유사도 방식의 근본적 한계로 판단해 **Google Gemini 판단으로 교체**(다른 워크플로우와 동일한 `Google Gemini 90일 무료(7/14~)` 크리덴셜 재사용). `T4`(주가 제외 + URL·제목 완전중복 제거까지만 담당) → `T4B`(AI에게 기사 인덱스 목록을 주고 같은 사건끼리 `CLUSTER: 0,2,4` 형식의 텍스트 줄로만 묶으라고 지시 — JSON.parse를 아예 안 쓰는, 이 프로젝트에서 관례적으로 써온 안전한 출력 포맷) → `T4C`(AI 응답 파싱 + 클러스터별 최신 기사를 대표로 선정 + 최신 5건, AI가 놓친 기사는 단독 클러스터로 보정) 구조로 재구성.
      - **썸네일 이미지 추가 + 포털 스타일 레이아웃**: 텍스트만 있어 밋밋하다는 피드백에 따라, `client_trend_articles`에 `image_url` 컬럼 추가하고 Serper 응답의 `imageUrl`을 n8n(T4→T4C)이 그대로 저장하도록 연결(Naver는 이미지가 없어 null → 프론트에서 신문 아이콘으로 대체). 카드를 이미지 배너(위) + 출처 배지·제목·요약(아래) 구조로 재구성하고, 좌우 버튼은 카드 전체가 아니라 이미지 위에 반투명 원형으로 올려 텍스트와 안 겹치게 함. 버튼이 슬라이드마다 새로 렌더링되므로 정적 id 바인딩 대신 `#trendCarouselBody` 이벤트 위임으로 변경, 기사 링크는 카드 전체가 아니라 제목에만 걸어 버튼-링크 중첩 클릭 문제를 원천적으로 피함.
      - **이미지 잘림 수정(`377c577`)**: 첫 배포 후 실제로 보니 `object-fit:cover`가 로고 합성형 이미지(예: "BROADCOM | SAMSUNG" 배너)를 가운데만 잘라 글자가 잘려 보임 → `object-fit:contain`(+ 여백 패딩)으로 교체해 이미지 전체가 잘림 없이 보이도록 수정. **이 커밋 이후 GitHub Pages 배포가 막혀서 아직 실제 사이트에는 반영 안 됨 — 8번 섹션 참고.**

37. **GitHub Pages 배포가 멈춰서 실제 사이트가 구버전에 멈춰있던 문제 (해결됨)** — `377c577`(이미지 잘림 수정) 커밋을 푸시한 뒤 GitHub Pages 배포가 계속 실패해서, 실제 라이브 사이트(`bpodsplan1.github.io/Research-On/`)가 그 이전 버전(`21da13f`, object-fit:cover인 상태)에 멈춰있었음. 코드 자체는 계속 정상적으로 커밋·푸시되고 있었고, 순수하게 GitHub Pages 배포 인프라 문제였음.
    - 증상 순서: (1) `377c577` 배포가 10분 넘게 `in_progress`로 멈췄다가 `failure`로 종료 → (2) 이후 모든 재배포 시도가 "Deployment request failed ... due to in progress deployment. Please cancel 377c577c19749689053f10cb5b88aeb9b9717e95 first"라는 동일한 에러로 즉시(수 초 내) 실패 반복(`07209be`/`f23f6a9`/`957ea06` 3회) — GitHub Pages 내부 배포 잠금 시스템에 `377c577` 배포가 "진행 중"으로 계속 남아있는 것으로 추정(Actions 실행 자체는 이미 완료·실패로 끝났는데도). (3) 사용자가 Settings → Pages에서 Source를 "GitHub Actions"로 재설정(이 과정에서 잠시 접속 자체가 안 되는 현상도 있었음, 이후 복구됨) → 재푸시(`5951537`, `39cfd10`)했지만 이번엔 아예 워크플로우(`pages-build-deployment`)가 트리거조차 안 됨. (4) 몇 시간 대기 후 재푸시(`9fbfbbb`)하니 정상 트리거되어 배포 성공 — 결국 일시적인 GitHub 인프라 문제였던 것으로 결론.
    - 진단에 사용한 방법(다음에 비슷한 문제 생기면 재사용): GitHub REST API를 인증 없이 curl로 조회(`api.github.com/repos/bpodsplan1/Research-On/deployments`, `.../actions/runs`, `.../actions/workflows`) — 이 저장소가 public이라 read-only 조회는 토큰 없이도 됨. 실제 라이브 사이트에 반영된 커밋을 확인하려면 `curl -sI https://bpodsplan1.github.io/Research-On/styles.css`의 `last-modified` 헤더로 판별 가능.
38. **클라이언트 동향 캐러셀 — 이미지 배너 제거, 텍스트 중심으로 복귀** — 썸네일 이미지(로고 합성형 배너 등)를 넣어봤지만 사용자가 보기에 여전히 어색하다고 판단해 이미지 영역 자체를 제거. 좌우 버튼은 이미지 위가 아니라 카드 가장자리(흰색 원형, 다른 카드 버튼과 톤 통일)로 이동. `client_trend_articles.image_url` 컬럼과 n8n의 이미지 수집 로직은 그대로 남겨둠(당장 화면에 안 쓸 뿐 데이터는 계속 쌓임 — 나중에 다시 쓰고 싶어지면 프론트 렌더링만 되돌리면 됨).
39. **계정 관리 &gt; 비밀번호 초기화 — 워크플로우가 n8n에서 사라져 있던 것을 발견해 재생성 + CORS 문제 진단** — 실제로 버튼을 눌러보니 `NetworkError when attempting to fetch resource` 발생. 원인 두 가지를 진단함: (1) 이전 세션에서 만들었다고 문서에 기록된 `[Research On] 관리자 - 계정 비밀번호 초기화` 워크플로우가 로컬 파일로 export된 적이 없었는데, 실제로 n8n에서 확인해보니 사라져 있었음(curl로 직접 웹훅을 호출해보니 `404 not registered` 응답으로 확인) — **n8n에서만 존재하고 저장소에 커밋 안 된 워크플로우는 이렇게 유실될 수 있다는 걸 보여준 사례.** (2) 설령 워크플로우가 살아있었어도 Webhook 노드에 CORS(Allowed Origins) 설정이 없어서, 브라우저가 보내는 preflight(OPTIONS) 요청이 n8n에서 500 에러로 거부당해 애초에 요청 자체가 안 나갔을 것(curl 직접 호출은 CORS 검사가 없어 정상 응답하지만, 브라우저에서만 막히는 게 특징) — 진단 시 `curl -X OPTIONS`로 preflight를 직접 재현해 확인함. `[Research On] 관리자 - 계정 비밀번호 초기화.json`을 새로 만들어 **이번엔 저장소에 커밋**하고 Allowed Origins를 `*`로 설정해 CORS 문제까지 함께 해결. n8n 가져오기·크리덴셜 연결·활성화는 아직 미착수(8번 섹션 참고). 같은 세션에 만들어진 "개별 구독자 강제 발송" 웹훅도 동일 패턴이라 CORS 누락 가능성 있어 다음에 점검 필요.
40. **일반 사용자 사이드바에서 "관리자 권한 신청" 메뉴 제거 + 사이드바 헤더 크기·정렬 개선** — 일반 사용자에게 셀프서비스 관리자 신청 메뉴가 굳이 필요 없다는 판단으로 nav 항목·페이지·관련 JS(`handleAdminRequest`/`loadMyRequestStatus`) 제거(관리자 쪽 승인 화면은 그대로 유지). 사이드바 상단 "OOO님 환영합니다" 줄을 톱니바퀴·접기 아이콘과 같은 행에서 정확히 정렬하고(기존엔 두 줄 텍스트 블록 전체 기준으로 가운데 정렬돼 첫 줄과 어긋나 보였음), 안내 문구("오늘도 행복 가득한...")는 별도 행으로 분리해 잘리지 않고 전부 표시되게 함 + 크기 확대. 톱니바퀴 아이콘(17px→20px)과 대시보드 바로가기 카드 이모지 아이콘(27px→32px)도 가시성 위해 확대.
41. **계정 설정 &gt; 문의/오류신고 내역에 삭제 기능 추가 + 무답변 처리완료 안내 문구** — 접수됨(미답변) 건에는 삭제 버튼, 답변 완료 건에는 비활성화된 "삭제 불가" 더미 버튼을 날짜 아래에 배치. `support_requests`에 DELETE 정책 신규 추가(본인 소유 + 미답변 건만 삭제 가능 — 프론트에서 막아도 API 직접 호출로 우회 가능하므로 RLS로도 동일하게 제한). 관리자가 답변 내용 없이 상태만 "답변 완료"로 바꾼 경우를 대비해, 빈 답변란 대신 "관리자가 답변을 남기지 않고 처리 완료하였습니다..." 안내 문구를 회색 이탤릭으로 표시(실제 답변과 시각적으로 구분).
42. **n8n 스케줄 트리거에 시간대(Asia/Seoul) 명시적 고정** — 사용자가 "Supabase에 저장되는 시간이 어느 시간대냐"고 질문한 게 계기. 확인 결과: (1) Supabase에 저장되는 타임스탬프 자체는 `new Date().toISOString()`을 써서 이미 항상 정확한 UTC로 저장되고 있어 문제없음. (2) 그런데 더 중요한 걸 발견 — "매주 월요일 08시 발송"(뉴스레터 2단계), "매주 목요일 08시"(뉴스레터 1단계), "매일 07시"(클라이언트 동향) 같은 **스케줄 트리거들이 시간대를 전혀 지정하지 않고 있었음**. n8n은 이 경우 인스턴스 전역 기본 시간대를 따르는데, 그게 명시적으로 `Asia/Seoul`로 설정 안 돼 있으면(n8n 기본값은 흔히 `America/New_York`) "08시"가 실제로는 한국 시간 기준 08시가 아닐 수 있는 잠재적으로 심각한 버그였음(뉴스레터가 엉뚱한 시각에 발송될 위험). 스케줄이 있는 4개 워크플로우(뉴스레터 1/2단계, 클라이언트 동향, 웰컴메일) 전부 `settings.timezone`을 `Asia/Seoul`로 명시적으로 고정해, n8n 인스턴스 설정과 무관하게 항상 한국 시간 기준으로 실행되도록 함. n8n 재가져오기 후 실제 발송/실행 시각이 의도대로(월요일 08시 등) 맞는지 다음 세션에서 확인 필요.
43. **로그인 화면에 아이디 찾기 / 비밀번호 초기화 셀프서비스 신설** — 로그인 패널에 링크 2개 추가, 클릭하면 회원가입 패널로 넘어가는 것과 동일한 방식으로 같은 카드 안에서 전용 화면으로 전환되고, 좌측 상단 "돌아가기" 버튼으로 로그인으로 복귀.
    - **아이디 찾기**: 성명+이메일 입력 → 일치하면 아이디를 마스킹(앞 2자/뒤 2자만 노출, 나머지 `*`)해서 보여줌. `profiles`가 RLS로 잠겨있어 로그인 전(anon)에는 직접 조회가 안 되므로, 이것만을 위한 좁은 RPC `find_masked_user_id(name, email)` 신규 생성(anon 실행 권한).
    - **비밀번호 초기화**: 관리자 승인 절차 없이, 아이디+성명+이메일 3개가 모두 일치하면 즉시 기본 비밀번호(`dlxmsjtm1!`)로 초기화하고 팝업으로 안내(일반적인 이메일 인증 기반 "비밀번호 찾기"는 구현 난이도상 보류하고 사용자가 직접 요청한 단순화된 방식). 신규 n8n 웹훅 `[Research On] 사용자 - 셀프 비밀번호 초기화` 생성 — 관리자용 비밀번호 초기화 워크플로우와 거의 동일한 구조(Supabase Auth Admin API 호출)이되, 인증 방식이 `admin_token`이 아니라 "3개 필드가 DB의 한 행과 정확히 일치하는지"로 다름.
    - **보안 트레이드오프 인지**: 아이디+성명+이메일 3개만 맞으면(전부 사내에서 알려질 수 있는 정보) 누구나 비밀번호를 기본값으로 초기화할 수 있음 — 사용자가 이 단순화를 명시적으로 요청했고, 대상이 사내(@etners.com) 한정 도구라는 점을 감안해 그대로 구현. 더 강한 보안이 필요해지면 이메일 인증 등 추가 검증 단계를 나중에 얹을 수 있음.
    - **실사용 검증 중 발견·수정한 버그 3건 (같은 세션 내)**:
      1. **불일치해도 "성공"으로 뜨던 버그** — n8n이 매칭 실패 시 HTTP 200 + 완전히 빈 응답(0바이트)을 반환하는 경우가 있었는데, 프론트가 "success===false가 아니면 성공"이라는 opt-out 방식이라 빈 응답도 성공 처리해버림. 프론트를 "success===true라고 명시해야만 성공"으로 반전(같은 패턴이던 관리자 비밀번호 초기화·뉴스레터 강제발송도 함께 수정).
      2. **n8n 쪽 근본 원인 수정** — 처음엔 "일치하는 계정이 없으면 더미 UUID(`00000000-...`)로 Auth Admin API를 호출해 자연스럽게 실패시키는" 방식으로 설계했는데, continueOnFail 이후의 에러 형태를 정확히 예측하지 못해 응답이 비어버리는 원인이 된 것으로 추정됨. **IF 노드로 명확히 분기**해서, 일치하는 계정이 확인된 경우에만 Auth Admin API를 호출하도록 재구성(불일치 시엔 API 호출 자체를 안 함).
      3. **실패 사유를 구체적으로 안내** — 처음엔 실패 메시지가 "일치하는 계정을 찾을 수 없습니다" 하나뿐이었는데, 사용자가 "어떤 필드가 틀렸는지 알려달라"고 요청. 아이디로만 먼저 조회한 뒤 성명/이메일을 서버에서 직접 비교해서 "아이디를 찾을 수 없습니다" 또는 "성명·이메일이 일치하지 않습니다"처럼 구체적으로 안내하도록 변경(다만 이렇게 하면 아이디 존재 여부가 노출되어 계정 추측에 악용될 여지가 소폭 있음 — 사내 전용 도구라는 전제로 감수).
      - 디버깅 과정에서 발견한 사실: **사용자가 이 워크플로우를 별도 파일로 가져오지 않고, 기존 `[Research On] 관리자 - 계정 비밀번호 초기화` 워크플로우 캔버스에 노드를 수동으로 병합**해서 씀. 그래서 이후 이 워크플로우를 다시 수정할 때는 파일 재가져오기보다 n8n 캔버스에서 해당 노드들만 직접 찾아 코드를 교체하는 방식으로 안내해야 함.
    - n8n 크리덴셜 연결·활성화는 완료됨(사용자가 직접 병합해서 진행). 다만 마지막 버그 수정 3번(필드별 실패 메시지, 커밋 `670cf82`)은 n8n 캔버스에 아직 반영 안 됐을 수 있음 — 8번 섹션 참고.
44. **사이드바/대시보드 UI 크기 미세조정 — 여러 차례 시행착오 끝에 진짜 원인 발견** — 사용자가 스크린샷으로 계속 피드백을 주며 여러 차례 반복한 조정. 최종적으로 확정된 값과, 그 과정에서 배운 것:
    - **사이드바 헤더**: 환영 문구 `18px`, 안내 문구("오늘도 행복 가득한...") `16px`(별도 줄로 분리, 잘리지 않고 전체 표시), 아이콘 버튼(톱니바퀴·접기) `38px` 박스에 폰트 `20px`. 한 차례 더 키웠다가(`22px`/`44px`) 사용자가 "환영 문구·아이콘은 원래 크기가 낫다"고 해서 되돌리고, 안내 문구만 키운 채로 확정.
    - **대시보드 바로가기 이모지(중요, 재발 방지용 기록)**: 최종적으로 `font-size:38px`(모바일 `25px`)만으로 해결됨 — **`transform:scale()`이나 `translate()` 같은 보정은 전혀 필요 없었다.** 처음에 "font-size를 52px까지 키워도 이모지가 안 커 보인다"는 문제의 **진짜 원인은 이모지 렌더링 특성이 아니라, 라벨 텍스트("새 리서치" 등)용으로 만든 `.dash-shortcut span{font-size:13.5px}` 규칙이 일반 span 선택자라서 새로 추가한 이모지 span까지 같이 덮어쓰고 있었던 CSS 우선순위 버그**였음(class+태그 선택자가 단일 class 선택자보다 우선순위가 높음). 이걸 모른 채 `scale()`/`translate()`로 억지로 우회하려다가 이모지가 박스 밖으로 커지거나(스케일을 박스 자체에 걺), 좌하단으로 쏠려 보이거나(스케일 중심이 안 맞음), 3번째 아이콘(◆, 컬러 이모지 아닌 일반 텍스트 기호라서 원래 문제 없었음)까지 같이 틀어지는 등 여러 겹의 문제를 만들었었음. 우선순위 버그를 라벨 span 전용 클래스(`.dash-shortcut-label`)로 분리해 해결한 뒤에는, 스케일 트릭을 전부 걷어내고 순수 `font-size`만으로 깔끔하게 해결됨. **교훈: 이모지/아이콘이 "안 커 보인다"는 문제는 렌더링 특성을 의심하기 전에 CSS 우선순위 충돌부터 devtools로 확인할 것.**
    - 대시보드 바로가기 카드에 한때 있었던 이미지 썸네일(38번 항목 참고)과는 별개로, 바로가기 아이콘은 처음부터 끝까지 이모지 문자였음.
45. **웹사이트 기능 개발은 이 시점에서 일단락** — 사용자가 결과보고 자료 준비 단계로 넘어가기 위해 요청. 이 문서(`research_on_project_context.md`)를 결과보고 작성의 근거 자료로 쓸 예정. 이후 세션은 보고서 작성 지원, 또는 8번 섹션에 남은 실사용 검증·설정 항목 위주가 될 가능성이 높음.
46. **담당자 테스트 피드백 9건 반영** — 결과보고 준비 중 담당자들이 실사용 테스트를 진행해 전달한 오류/수정/건의 목록을 하나씩 코드로 원인 확인 후 반영.
    - **자료 중복 저장 오류 수정** — `saveResultDoc`/`saveAllResults`(js/research-results.js)와 `saveCurrentReport`(js/insight-report.js)가 기존 저장 목록과 대조 없이 무조건 insert하고 있었음. `js/keywords-state.js`에 공용 `isAlreadySaved(title, type)` 헬퍼를 추가해 저장 전 중복 체크 후 "이미 저장된 자료입니다" 안내로 대체.
    - **뉴스레터 관리자 편집 버튼이 실제 발송본에도 섞여 들어가던 문제 (제보 내용보다 범위가 컸음)** — `nlItemBlock`/`nlSectionBlock`/`nlCultureViewBlock`(js/newsletter-admin.js)가 관리자 미리보기 iframe과 `buildNewsletterHtml`(실제 발송·저장되는 최종 HTML)에 동일하게 재사용되고 있어서, ▲▼/편집/삭제/항목추가 버튼이 **실제로 구독자에게 발송되는 이메일에도** 그대로 박혀 있었던 구조적 버그. `editable` 플래그를 추가해 관리자 미리보기(`refreshNewsletterPreview`)에서만 `true`로 넘기고, 실제 발송(`sendNewsletter`)에서는 기본값(false)으로 완전히 제거되도록 분리.
    - **고급 옵션 "검색 범위"(시작일/종료일) 모바일 화면 잘림** — CSS Grid의 기본 동작(`min-width:auto`)때문에 `input[type=date]`가 그리드 트랙보다 좁아지지 못해 카드 밖으로 삐져나오던 문제. `styles.css`에 `.field{min-width:0}`와 `input,select,textarea{min-width:0;box-sizing:border-box}` 추가로 해결(이 규칙을 쓰는 다른 폼 그리드에도 공통 적용됨).
    - **키워드 최종조합 모바일 드래그 순서 변경 불가** — `bindComboDragEvents`(js/config.js)가 `mousedown/mousemove/mouseup`만 바인딩돼 있어 터치 이벤트가 아예 없었음. Pointer Events(`pointerdown/pointermove/pointerup` + `setPointerCapture`)로 교체해 마우스/터치 공용으로 동작하도록 수정, `#selectedTags .chip`에 `touch-action:none` 추가(드래그 중 페이지 스크롤과 충돌 방지).
    - **대상 국가 다중 선택 — 보류 확정** — 프론트만 배열로 바꾸면 되는 게 아니라, 뉴스크롤링 1단계 워크플로우가 Serper `gl` 파라미터(국가 1개만 지원)를 단일 문자열로 쓰고 있어 다중 선택을 실제로 반영하려면 n8n에서 국가별 병렬 검색 후 병합하는 구조 변경이 필요함을 확인. 사용자가 "국가 1개씩만 선택"으로 현행 유지 결정 — 코드 변경 없음.
    - **구독 신청 안내 문구 정리** — "※ 아래 내용은 임시 예시이며..." 문구와 TODO 주석을 제거하고, 실제로 확정된 뉴스레터 4개 섹션(CLIENT WATCH/RESEARCH ON INSIGHT/BUSINESS & WORK/PEOPLE & CULTURE) 설명으로 교체(index.html).
    - **관리자 뉴스레터 구독 관리에 외부(비가입) 구독자 미표시 수정** — `loadNewsletterSubscribers`(js/admin.js)가 `profiles` 테이블만 조회해서, 대리 등록된 비가입 이메일이 쌓이는 `newsletter_guest_subscribers`가 관리 화면에 전혀 안 보이고 있었음. 두 테이블을 함께 조회해 병합하도록 수정하고, 일시정지/구독취소 버튼도 행의 출처(`kind: 'profile'|'guest'`)에 따라 각각 다른 테이블·컬럼(`profiles.newsletter_paused` vs `newsletter_guest_subscribers.status`)을 갱신하도록 분기. **검증 필요**: `newsletter_guest_subscribers`에 대한 RLS가 관리자(`authenticated`+`is_admin()`)의 전체 행 SELECT/UPDATE를 허용하는지 미확인 — 기존에는 INSERT(대리 등록)만 쓰이고 있었어서, 권한이 그것만 열려 있었다면 이 화면에서 여전히 비어 보이거나 수정이 실패할 수 있음.
    - **AI 인사이트 리포트 탭 표현 수정** — "무슨 일인가"는 CL4+ 보직자 대상 격식에 비해 구어체라는 피드백에 따라 "개요"로, 같은 결의 "왜 중요한가"도 "분석"으로 함께 정리(둘 다 n8n 워크플로우 `[Research On] 뉴스 크롤링 - 2단계 ...json`의 `8. Code` 노드에 하드코딩돼 있던 라벨 — **n8n 재가져오기 필요**, 아직 미착수).
    - **뉴스레터 구독 해지 안내 메일 신설** — 기존 "신규 구독 웰컴 메일"과 대칭되는 새 n8n 워크플로우 `[Research On] 뉴스레터 - 구독 해지 안내 메일.json`을 웰컴 메일과 동일한 구조(Webhook → 본문 구성 → Gmail 발송 → 응답)로 신규 작성(웹훅 경로 `newsletter/goodbye`). `js/config.js`에 `N8N_NEWSLETTER_GOODBYE_URL` 추가, `unsubscribeNewsletter()`(js/newsletter-admin.js)에서 해지 처리 직후 트리거하도록 연동. **n8n에 아직 가져오기 전 — Gmail 크리덴셜 연결 + 활성화 필요.**

---

## 8. 진행 중 / 미해결 사항

- ~~**newsletter_guest_subscribers UPDATE 권한 누락**~~ → 2026-07-27 해결. 관리자 화면에서 외부(비가입) 구독자 "일시 정지"/"구독 취소"를 누르면 `permission denied for table newsletter_guest_subscribers` 오류 발생 — 이 테이블에 INSERT/SELECT 권한만 있고 UPDATE 권한 자체가 없었음(`newsletter_send_history`와 동일 패턴). `is_admin()` 조건의 UPDATE 정책 추가로 해결, 실제 운영 DB에도 적용 완료.
- ~~**"발송하기" 이후 실제 이메일 발송 여부는 n8n 스케줄에 달려있음**~~ → 2026-07-27 확인 및 정리 완료. 테스트 중 "발송하기"를 눌러도 실제 이메일이 안 가는 것처럼 보인다는 문의가 있어 Supabase를 직접 조회해 확인함: `newsletter_send_history`(관리자 앱의 "최신 뉴스레터" 표시용)와 `newsletters`(n8n이 실제 발송 대기열로 쓰는 테이블, status=QUEUED) 양쪽 다 정상적으로 새 행이 생성됨 — "발송하기" 버튼과 n8n 큐잉 웹훅 자체는 정상 동작하고 있었음. 다만 실제 Gmail 발송은 설계상 즉시 일어나지 않고 **매주 월요일 08시(Asia/Seoul) 스케줄 트리거가 QUEUED 상태를 골라서 발송**하는 구조(3번 섹션 [C] 참고)인데, `newsletters` 테이블에 당일 SENT 행도 섞여 있어 원인을 물었더니 사용자가 "관리자 승인 직후 1분 뒤 자동 발송"과 "신규 구독 웰컴 메일 5분 뒤 최신호 발송"을 혼동하고 있었음 — **완전히 다른 두 기능**이라고 설명함(전자: `[Research On] 뉴스레터 - 2단계` 워크플로우의 `B-T*` 노드 3개, 관리자가 승인·예약할 때마다 1분 뒤 예약시각을 "지금"으로 앞당겨 곧장 발송 파이프라인(C1~C6)을 태우는 테스트용 지름길. 후자: 별도 워크플로우 `[Research On] 뉴스레터 - 신규 구독 웰컴 메일 및 최신호 즉시 발송`의 웰컴 메일+5분 뒤 최신호 발송으로, 신규 구독자 온보딩용이라 계속 필요). 사용자가 전자(1분 뒤 자동 발송)는 불필요하다고 확인해 **`B-T. Wait - 승인 후 1분 대기 (테스트용)` / `B-T0. Code - 테스트용 발송시각 즉시화` / `B-T1. Google Sheets - 예약시각 테스트용 갱신` 3개 노드를 워크플로우 파일에서 완전히 제거**(B3의 출력이 이제 B4 한쪽으로만 나감). **n8n 캔버스에도 동일하게 반영(재가져오기 또는 노드 3개 수동 삭제) 필요 — 아직 미착수.**
- ~~**GitHub Pages 배포 정지 상태**~~ → 2026-07-25 밤 자연 해소됨(7번 섹션 37번 항목 참고). 몇 시간 대기 후 재푸시(`9fbfbbb`)했더니 워크플로우가 정상 트리거되어 배포 성공, 라이브 사이트에 이미지 잘림 수정본까지 정상 반영 확인함(`styles.css`에 `object-fit:contain` 확인됨). 결국 일시적인 GitHub 쪽 인프라 문제였던 것으로 결론.
- **n8n 스케줄 시간대 고정(Asia/Seoul) 검증 필요** (7번 섹션 42번 항목, 아직 미착수) — 4개 워크플로우(뉴스레터 1/2단계, 클라이언트 동향, 웰컴메일)에 `settings.timezone: "Asia/Seoul"`을 추가했으나 n8n에 재가져오기 전이라 아직 실제 적용 안 됨. 재가져오기 후 스케줄 트리거 다음 실행 예정 시각이 n8n UI에 한국 시간 기준으로 정확히 표시되는지 확인할 것.
- ~~**아이디 찾기 / 셀프 비밀번호 초기화 실사용 전 설정**~~ → 2026-07-26 완료(7번 섹션 43번 항목). 사용자가 관리자용 비밀번호 초기화 워크플로우 캔버스에 직접 병합해서 크리덴셜 연결·활성화까지 마침, 실제 성공 케이스도 확인됨.
- **셀프 비밀번호 초기화 — 필드별 실패 메시지 수정(커밋 `670cf82`) n8n 캔버스 반영 확인 필요** (7번 섹션 43번 항목) — 로컬 파일은 최신이지만, 사용자가 워크플로우를 파일로 가져오지 않고 n8n 캔버스에 수동 병합해서 쓰고 있어 이 마지막 수정이 실제 n8n에 반영됐는지 불확실. `U2`(쿼리를 아이디만으로 조회하도록 변경)·`U3`·`U7` 3개 노드의 코드를 n8n 캔버스에서 직접 교체해야 함(다음 세션에서 확인 또는 안내).
- **뉴스레터 키워드 확장(42개) 시 토큰 영향 검토** — 논의 중 중단됨. 현재 검색 Job은 Google Sheets의 키워드 목록(각 행: section/category/target/keyword)을 기반으로 생성되며, 키워드 수가 늘어나면 그만큼 검색 Job도 늘어나 원본 후보 풀이 커진다. 이것이 실제로 AI 평가 단계 토큰에 얼마나 영향을 주는지는 실제 키워드 목록을 다시 확인해서 계산 필요.
- ~~**HITL(관리자 검토) 설계 고도화**~~ → 이번 세션에서 실시간 편집 미리보기 방식으로 개편 완료(7번 섹션 17번 항목). 다만 "AI가 상위 몇 건을 추리고 사람이 최종 선택"하는 단계별 선택 UX까지는 아직 아님 — 현재는 "AI가 초안을 다 채워주면 사람이 그 위에서 자유롭게 더하고 빼고 순서를 바꾸는" 방식.
- ~~**Supabase RLS 정책 미확인**~~ → 2026-07-25 전면 점검·적용 완료(7번 섹션 31번 항목, 6번 섹션 테이블 참고). 심각한 취약점이었고 지금은 해결됨.
- **Serper API 크레딧 소진 이슈 관찰됨** — 실행 로그에서 "Not enough credits" 에러 확인. 계정 잔여 크레딧/결제 상태 확인 필요. (People & Culture 검증 단계는 크레딧 절약을 위해 Naver에서 이미 관련 자료를 찾으면 SNS 보완검색을 건너뛰도록 조건부로 설계되어 있음) **주의**: 36번 항목의 "클라이언트 동향" 워크플로우가 매일 07시에 Serper 호출을 추가로 1건씩 발생시키므로, 크레딧이 빠듯하면 이 워크플로우 활성화 전에 먼저 크레딧 상태부터 확인할 것.
- **"클라이언트 동향" 캐러셀** — 수동 1회 실행(Execute Workflow)으로 테스트 완료, `client_trend_articles`에 실제 기사 5건 저장되고 대시보드 캐러셀에 정상 표시되는 것까지 확인됨(썸네일 이미지 포함). **남은 것은 매일 07시 자동 실행을 위한 활성화(Active)뿐** — n8n에서 이 워크플로우를 Active로 전환해야 매일 자동 갱신된다.
- ~~**CSS/JS 파일 분리**~~ → 2026-07-25 완료(7번 섹션 29번 항목). **Docker 배포 준비는 여전히 미착수.**
- **계정 관리 &gt; 비밀번호 초기화 — 워크플로우가 n8n에서 사라져 있던 것을 발견해 재생성함** (2026-07-25, 7번 섹션 38번 항목) — 실제로 버튼을 눌러보니 `NetworkError when attempting to fetch resource`가 발생. 진단해보니 두 가지 문제였음: (1) 이전 세션에서 만들었다던 `[Research On] 관리자 - 계정 비밀번호 초기화` 워크플로우가 로컬 파일로 export된 적이 없어서, n8n에서 없어진(또는 애초에 저장 안 된) 상태였고 웹훅 자체가 `404 not registered` 응답을 내고 있었음. (2) 설령 활성화돼 있었더라도 Webhook 노드에 CORS(Allowed Origins) 설정이 아예 없어서 브라우저 preflight가 500으로 막혀 애초에 요청이 안 나갔을 것 — curl로는 정상 응답하지만 브라우저에서만 막히는 게 이 증상의 특징. `[Research On] 관리자 - 계정 비밀번호 초기화.json`으로 새로 만들어 **저장소에 커밋**(다시는 이렇게 사라지지 않도록) + Allowed Origins를 `*`로 설정해 재생성함. **아래 설정 후 실사용 가능**(아직 미착수):
  - n8n에 이 파일을 가져오기(import)
  - `W2. HTTP Request` 노드에 `Supabase` 크리덴셜(secret 키, `apikey` 헤더)이 연결됐는지 확인 — C5B 노드가 쓰는 것과 동일한 크리덴셜 재사용
  - 워크플로우 **활성화(Active)**
  - **"개별 구독자 강제 발송" 웹훅도 같은 세션에 만들어진 동일 패턴이라 CORS가 똑같이 빠져있을 가능성이 높음 — 다음 세션에서 같이 점검할 것.** (`[Research On] 뉴스레터 - 2단계`의 D0~D6 노드)
  - 두 웹훅에 적용된 최소 방어용 `admin_token`(현재 값: `ro-admin-8f2c1d9e41`)은 완전한 인증이 아니므로, 실제 운영 전 호출자의 Supabase 세션(JWT)을 서버 단에서 검증하는 절차 추가를 검토할 것
- ~~**GitHub Pages 배포 시 `index.html` 파일명 웹 업로드 실패 이슈**~~ → 2026-07-25 git(SSH 키 인증)로 저장소를 직접 연결해 해결(7번 섹션 28번 항목). 이제 `git push`로 올리므로 웹 UI 드래그 업로드 자체를 안 씀.
- **뉴스레터 신규 구독 웰컴 메일 — 실사용 전 설정 필요** → `[Research On] 뉴스레터 - 신규 구독 웰컴 메일 및 최신호 즉시 발송` 워크플로우를 n8n에서 활성화(Active)해야 실제로 동작함(아직 미착수). 웰컴 메일 문구는 참고 파일 없이 직접 작성한 초안이므로, 실제 톤·포맷을 맞출 참고 자료가 있으면 다시 다듬을 것.
- **n8n [뉴스레터 2단계] 신규 노드(`C5B`) 활성화 전 설정 필요** (7번 섹션 33번 항목) — 위 비밀번호 초기화용과 동일한 Supabase secret 키 크리덴셜을 새 노드에 연결하고, 파일을 n8n에 재병합 후 활성화해야 `newsletter_last_send_status` 배지가 실제로 채워짐. 실패(발송 자체가 안 된 경우) 상태는 이번 범위에서 다루지 않음 — 필요해지면 별도 에러 브랜치 설계부터 다시 논의할 것.
- **키워드 세트(`keyword_sets`) 저장 UI는 만들지 않기로 확정** (7번 섹션 34번 항목) — `searchKeywordSet`/`deleteKeywordSet`는 죽은 코드로 남겨둠. 나중에 필요해지면 그때 다시 설계.
- ~~**n8n Sheets/Docs → Supabase 이관 후속 작업**~~ (7번 섹션 35번 항목) → **데이터 이관 완료 확인됨**(2026-07-25). `[Research On] 1회성 - Sheets 데이터 Supabase 이관.json` 실행 완료, 실제 테이블 행 수 확인함: `newsletter_keywords` 42건, `newsletter_candidates` 48건, `research_reports` 23건, `newsletters` 9건, `context_docs` 3건. 이 과정에서 발견·수정한 버그 3건은 35번 항목에 기록(on_conflict 제약 없음, service_role 기본 권한 누락, 여분 필드로 인한 Bad Request). **이 1회성 이관 워크플로우는 다시 쓸 일 없으니 n8n에서 삭제 권장.**
  - **뉴스레터 2단계**는 재가져오기 후 실제 실행까지 사용자가 확인함(후보 초안 조회 A0~A8, 승인·예약 B0~B5, 발송 C0~C6 전부 성공) — Supabase 연동 정상 작동 확인.
  - **뉴스레터 1단계 / 뉴스크롤링 1·2단계 / 웰컴메일**은 JSON 수정은 완료됐지만 **아직 n8n 재가져오기 후 실제 실행 테스트는 안 됨** — 다음 세션에서 순서대로 재가져오기 + 수동 1회 실행해서 확인 필요. 특히 뉴스레터 1단계 실행 후 `newsletter_candidates`에 새 행이 쌓이는지 확인할 것.
  - 검증 끝나면 Google Sheets("Research On DB")와 Google Docs 3종은 백업용으로만 남기고 더 이상 수동으로 갱신하지 않아도 됨(맥락 문서를 고치고 싶으면 이제 Supabase `context_docs` 테이블에서 수정).
- **신규 워크플로우 `[Research On] 뉴스레터 - 구독 해지 안내 메일` 실사용 전 설정 필요** (7번 섹션 46번 항목) — n8n에 가져오기(import) + Gmail OAuth2 크리덴셜 연결(웰컴 메일과 같은 크리덴셜 `Gmail(7/14)` 재사용 가능) + 활성화(Active) 필요. 아직 미착수.
- **뉴스 크롤링 2단계 워크플로우 탭 라벨 수정("무슨 일인가"→"개요", "왜 중요한가"→"분석") n8n 재가져오기 필요** (7번 섹션 46번 항목) — 로컬 파일만 수정된 상태, 실제 n8n 캔버스에 반영해야 화면에 새 라벨로 보임.
- **`newsletter_guest_subscribers` RLS 권한 확인 필요** (7번 섹션 46번 항목) — 관리자 뉴스레터 구독 관리 화면에서 이 테이블을 SELECT/UPDATE 하도록 프론트를 고쳤지만, 기존 RLS가 INSERT만 허용하고 있었을 가능성이 있어 실제로 목록이 뜨고 일시정지/구독취소가 되는지 실사용 확인 필요. 안 되면 `is_admin()` 기준 SELECT/UPDATE 정책 추가 필요(`supabase/migrations/` 참고).
- **이번 세션(2026-07-24~25) 변경사항 실사용 검증 필요** — 아래는 전부 샌드박스(헤드리스 브라우저·로컬 라이브러리 실행·Supabase RLS 시뮬레이션 쿼리)로 로직 단위까지는 검증했지만, 실제 배포 환경·실사용자 기준 확인은 아직 안 됨: 사이드바 로그아웃 버튼(100dvh 수정, 24번), 리서치 결과 카드 전체클릭 선택 + 상세보기 버튼(25번), PDF 섹션 잘림 방지(25번), 회원가입 부서/직위 저장(26번), 부서 경로 미리보기(27번), **파일 분리 후 전체 기능 정상 동작 여부(29번)**, **저장 자료함 다중 선택 삭제(30번)**, **RLS 적용 후 기존 관리자 기능들이 전부 정상 동작하는지(31번 — 특히 계정 관리/뉴스레터 구독 관리 화면)**, **지원 및 문의 내역확인·관리자 답변(32번)**.

---

## 9. 작업 방식 원칙 (합의된 규칙)

- **워크플로우(n8n) 파일은 사용자 승인 없이 새로 만들지 않는다** — 먼저 대화로 방안을 정한 뒤 반영.
- 버그 수정은 **프롬프트 지시보다 코드 레벨 안전장치를 우선**한다(반복적으로, 프롬프트만으로 "더 정확하게/더 많이/덜 뭉뚱그리게" 요청하는 방식은 신뢰도가 낮다는 게 이 프로젝트에서 여러 번 확인됨). 이번 세션의 People & Culture 검색어 한국어화, 관련성 필터, `runOnceForEachItem` 누락 버그 등도 전부 이 원칙에 따라 코드 레벨에서 해결함.
- 수정 전 반드시 실제 데이터(첨부된 .eml, n8n 실행 결과 JSON, 관리자 화면 스크린샷 등)로 원인을 확인한 뒤 처방한다 — 추측성 수정 지양.
- 수정 후에는 실제 데이터 또는 재현 테스트로 검증한 뒤 전달한다. (CSS/HTML/JS 문법 검증, Node.js로 실제 함수를 실행해 결과 데이터 직접 확인, 필요 시 실제 렌더링 엔진으로 스크린샷까지 찍어 확인하는 방식을 반복적으로 사용함)
