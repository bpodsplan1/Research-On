# Research On — 배포 가이드

이 문서는 개발이 끝난 시스템을 실제 운영 환경에 올리는 절차를 정리한다. 기능 개발 관련 맥락은
`research_on_project_context.md`를 참고하고, 이 문서는 순수하게 "어디에, 어떻게 올리는가"만 다룬다.

---

## 1. 배포 대상 정리 (프론트/백엔드는 완전히 별개의 두 작업)

| 구성요소 | 실체 | 배포 위치 | 담당 |
|---|---|---|---|
| 프론트엔드 | 정적 HTML/CSS/JS (`index.html`+`styles.css`+`js/*.js`) | **Cloudflare Pages** | 본인 |
| 백엔드(n8n) | n8n 워크플로우 7개(검색·AI분석·메일발송) | **AWS 서버**(EC2 등)에 Docker로 자체 호스팅, 도메인 `mokai.kr`(추정) | 직장 상사 |
| DB/인증 | Supabase | Supabase 자체 매니지드 호스팅 — 별도 배포 불필요 | - |

**핵심 오해 정정**: "AWS냐 Cloudflare냐"를 양자택일로 생각하기 쉬운데, 이 프로젝트는 원래 프론트와 백엔드를
따로 호스팅하는 게 정상 구조다. Cloudflare는 프론트(정적 파일)만 담당하고, 백엔드(n8n)는 AWS에서
계속 돌아간다. 두 곳은 서로 URL로만 연결되는 완전히 독립된 인프라다.

과거(2026-07-28 이전)에는 n8n이 `n8n.mokai.kr`이라는 외부 호스팅 주소로 이미 연결돼 있었고,
이 주소는 실제로는 상사가 AWS에 직접 운영하는 서버였던 것으로 추정됨. 새 도메인 구매를 논의 중이라
이 주소가 조만간 바뀔 수 있음 — 바뀌면 아래 4번 항목대로 프론트 코드도 함께 갱신해야 함.

---

## 2. 프론트엔드 배포 — Cloudflare Pages

**개념**: GitHub Pages와 동일한 개념의 정적 사이트 호스팅. Git 저장소를 연결해두면 push할 때마다
자동 재배포된다. 서버 개념이 없고 파일을 CDN에 올려주는 서비스라, 빌드 과정이 없는 이 프로젝트에
정확히 맞는다.

1. Cloudflare 대시보드 → `Workers & Pages` → `Create` → `Pages` → `Connect to Git`
2. GitHub 계정 연결 후 `bpodsplan1/Research-On` 저장소 선택
3. 빌드 설정 (빌드 과정 자체가 없으므로):
   - Framework preset: `None`
   - Build command: 비워둠
   - Build output directory: `/` (저장소 루트)
4. `Save and Deploy` → `research-on.pages.dev` 같은 임시 도메인 생성
5. (선택) `Custom domains` 탭에서 회사 도메인 연결 가능(도메인 DNS가 Cloudflare 네임서버에 있거나 CNAME 연결 필요)
6. GitHub Pages는 `/Research-On/` 서브경로였고 Cloudflare Pages는 루트 경로가 기본이지만,
   저장소 내 경로 참조가 전부 상대경로(`styles.css?v=...`, `js/xxx.js?v=...`)라 **코드 수정 없이 그대로 동작함**
   (2026-07-28 확인).

**요금**: 무료. 대역폭/요청 수 무제한, 커스텀 도메인 무료, 월 500회 빌드 한도(빌드가 없는 정적
사이트라 사실상 문제 안 됨). 카드 등록 없이 가입만으로 사용 가능.

**GitHub Pages 대비 유의점**: `research_on_project_context.md` 9번 섹션의 "정적 자산에
`?v=YYYYMMDDx` 캐시버스팅 쿼리스트링을 반드시 붙인다" 원칙은 Cloudflare Pages로 옮긴 뒤에도
동일하게 유지해야 함(Cloudflare도 정적 자산 캐싱을 적용하므로).

---

## 3. 백엔드(n8n) 배포 — AWS + Docker (상사 담당, 참고용 개념 정리)

n8n은 공식 Docker 이미지를 제공해서, 자체 서버에 컨테이너로 띄우는 게 표준적인 자체 호스팅 방법이다.
상사가 AWS 서버(EC2 등)에 이미 이 방식으로 n8n을 운영 중인 것으로 추정됨.

### 새 도메인 연결 시 맞춰야 하는 파라미터

도메인을 새로 사서 연결할 때 "파라미터가 달라서 연결이 안 된다"는 문제는, 아래 값들이 새 도메인
기준으로 전부 일치해야 하기 때문에 발생하는 게 일반적이다:

- **n8n 컨테이너 환경변수**: `N8N_HOST`, `N8N_PROTOCOL`, `N8N_PORT`, `WEBHOOK_URL`,
  `N8N_EDITOR_BASE_URL` — 특히 `WEBHOOK_URL`이 실제 웹훅 주소(`js/config.js`의
  `N8N_*_URL` 값들의 기준)를 결정하므로 가장 중요함
- **리버스 프록시 설정**(nginx/Caddy 등): `server_name`/도메인 지정이 새 도메인과 일치해야 함
- **SSL 인증서**: 새 도메인 기준으로 재발급 필요 (Let's Encrypt 등)
- **DNS**: 새 도메인의 A레코드(또는 CNAME)가 AWS 서버의 실제 IP를 정확히 가리켜야 함

이 중 하나라도 안 맞으면 연결 실패로 나타난다. 이건 AWS 서버 쪽 작업이라 이 프로젝트의 코드로는
손댈 수 없고, 상사가 서버에서 직접 맞춰야 하는 부분.

### 도메인이 확정되면 프론트에서 해야 할 일

새 n8n 도메인이 확정되면, [js/config.js](js/config.js)의 아래 값들을 전부 새 도메인으로 교체해야
프론트엔드가 정상 연결된다:

```
N8N_NEWS_SEARCH_URL / N8N_INSIGHT_URL / N8N_NEWSLETTER_DRAFT_URL / N8N_NEWSLETTER_QUEUE_URL
N8N_ADMIN_RESET_PW_URL / N8N_NEWSLETTER_FORCE_SEND_URL / N8N_SELF_RESET_PW_URL
N8N_NEWSLETTER_WELCOME_URL / N8N_NEWSLETTER_GOODBYE_URL
```

(현재 전부 `https://n8n.mokai.kr/webhook/...` 형태로 하드코딩돼 있음 — 도메인만 바뀌고 웹훅
경로(`/webhook/newsletter/queue` 등)는 n8n 워크플로우 쪽을 안 건드리는 한 그대로일 것으로 예상.)

---

## 4. 미해결 / 확인 필요

- 새 도메인 확정 여부 — 확정되면 `js/config.js`의 n8n URL 9개 교체 필요
- Cloudflare Pages 실제 배포 및 커스텀 도메인 연결 — 아직 미착수
- 배포 후 실사용 검증 필요 (프론트 이전 자체는 코드 수정이 없어 리스크 낮음)
